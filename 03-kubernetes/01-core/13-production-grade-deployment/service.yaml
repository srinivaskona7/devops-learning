apiVersion: v1
kind: Service
metadata:
  name: hello-prod
  labels:
    app: hello-prod
    component: web
spec:
  type: ClusterIP
  selector:
    app: hello-prod
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
