apiVersion: v1
kind: Service
metadata:
  name: obs-demo
  namespace: obs-demo
  labels:
    app: obs-demo
spec:
  selector:
    app: obs-demo
  ports:
    - name: http
      port: 80
      targetPort: 8000
      protocol: TCP
  type: ClusterIP
